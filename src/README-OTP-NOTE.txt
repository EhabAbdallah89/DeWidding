OTP in this package works locally on the browser for demo/testing.
It generates a 6-digit code and verifies it on the same device.
To switch to real SMS OTP later, connect Firebase Auth Phone provider and replace otpService with Firebase flow.
